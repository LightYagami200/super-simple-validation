is-cc-cvv
is-iban
is-eth-address
is-btc-address
is-sol-address
is-discord-bot-token (Optional parameter that lets you check if bot token is valid by making a request to Discord API)
is-discord-invite
is-discord-tag (Username + Discriminator e.g. "John#1234")
is-ipv4
is-ipv6
is-mac-address
is-imei

# Validations to Extend

is-url: An optional parameter of options,

```js
{
  test: true, // Test if the URL is valid by making a request to the URL
  requireProtocol: true, // Require the URL to have a protocol
  ssl: true, // Require the URL to be HTTPS
  type: 'image' | 'video' | 'audio' | 'document' | 'executable' | 'archive' | 'other' // Require the URL to be a specific type of file
}
```

is-phone: An optional parameter of options,

```js
{
  country:
}
```
